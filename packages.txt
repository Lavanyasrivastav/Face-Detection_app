# System libraries required by opencv-python / mediapipe on Streamlit
# Community Cloud's minimal Debian image. Without these, `import cv2`
# fails at runtime with an "libGL.so.1: cannot open shared object file"
# style error, even though pip install succeeds.
libgl1
libglib2.0-0
libsm6
libxext6
libxrender1
