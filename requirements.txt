# mediapipe>=0.10.21 removed the legacy `solutions` API in favor of the
# Tasks API (FaceLandmarker) that this project uses.
mediapipe>=0.10.21
opencv-python-headless>=4.9.0
numpy>=1.26

# Needed for app.py (Streamlit web demo). Streamlit Cloud only auto-detects
# a file named exactly "requirements.txt", so this is merged in here rather
# than kept solely in requirements-app.txt.
streamlit>=1.35
